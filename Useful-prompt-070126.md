Super, please improve previous design by adding following additional features: Please also adding 3 additional wow ai features to this app. Please make gemini-3.1-flash-lite default model for llm features (user can select other models and modify prompt). Please adding wow visualization effect for llm execution, wow interactive indicators, live log, wow interactive dashbpard with 5 wow graph. Please let user to select light/dark themes, default Traditional CHinese/english. please don't create code and only create a comprehensive technical specification in markdown in 4000 to 5000 words.  


Super, please improve previous design by adding following additional features: Please also adding 3 additional wow ai features to this app. please don't create code and only create a comprehensive technical specification in markdown in 4000 to 5000 words. ENding with 20 comprehensive follow up questions


Super, please improve previous design by adding following additional features: Implement a hook that loads the contents of 'dataset.md' upon application mount and stores it in the application state to be used as default data. Please also adding 3 additional wow ai features to this app. please fix blank screen bugs and iterate until get excellent results.  ENding with 20 comprehensive follow up questions 


Super, please improve previous design by keeping all original featurtes and adding additional features that 1. Please enhense "data core" feature that please let user to upload or paste datasets (json, csv, md), user can select type of datasets (ex. TUDID Dataset) then user can hit import to import the dataset. User can (optional) standardized datasets (defaullt gemini-3.1-flash-lite, user can select other models and modify prompt) before import the dataset. Please let user to preview the datasets (default 20 records, user can select which datasets to preview and how many records to preview). Also let user to export datasets ( user can select which datasets to export, csv/json/md). Please also create 6 graphs (by python code) to show datasets  ( user can select which datasets to use) and user can prompt (defaullt gemini-3.1-flash-lite, user can select other models and modify prompt) on the selected dataset. Enhance the search functionality with a fuzzy-match algorithm (e.g., using a library like Fuse.js) to improve result reliability when searching for medical device names with typos or variations. Add a smooth horizontal slide transition effect to the table content when switching between the six regulatory tabs in the main workspace. Integrate camera access to allow the user to scan device barcodes. Use the scanned UDI/ID to automatically trigger the 'AI Nomenclature Translate' flow for the matched record in the TUDID Registry. Implement a 'Generate Compliance Report' button in the AI Output container that uses the current result state to generate and download a PDF report containing all findings and mitigation steps. Please also adding 3 additional wow ai features to this app. Ending with 20 comprehensive follow up questions



Add advanced search and filtering capabilities to the application. Users should be able to search across different fields, use boolean operators, and apply multiple filters simultaneously to refine their search results effectively.

Design a customizable dashboard for the application. Users should be able to add, remove, and rearrange various widgets (e.g., recent activity, key metrics, quick links). Ensure the dashboard layout is responsive and adapts to different screen sizes.

Add functionality for users to export their data in multiple formats (CSV, JSON, PDF) and import data from common file types. Ensure the import process is robust and handles potential data inconsistencies gracefully.

Add advanced search and filtering capabilities to the application. Users should be able to search across different fields, use boolean operators, and apply multiple filters simultaneously to refine their search results effectively.

Design a customizable dashboard for the application. Users should be able to add, remove, and rearrange various widgets (e.g., recent activity, key metrics, quick links). Ensure the dashboard layout is responsive and adapts to different screen sizes.

Add functionality for users to export their data in multiple formats (CSV, JSON, PDF) and import data from common file types. Ensure the import process is robust and handles potential data inconsistencies gracefully.

Super, please improve previous design by keeping all original featurtes and adding additional features that 1. Please enhense "data core" feature that please let user to upload or paste datasets (json, csv, md), user can select type of datasets (ex. TUDID Dataset) then user can hit import to import the dataset. User can (optional) standardized datasets (defaullt gemini-3.1-flash-lite, user can select other models and modify prompt) before import the dataset. Please let user to preview the datasets (default 20 records, user can select which datasets to preview and how many records to preview). Also let user to export datasets ( user can select which datasets to export, csv/json/md). Please also create 6 graphs (by python code) to show datasets  ( user can select which datasets to use) and user can prompt (defaullt gemini-3.1-flash-lite, user can select other models and modify prompt) on the selected dataset. Enhance the search functionality with a fuzzy-match algorithm (e.g., using a library like Fuse.js) to improve result reliability when searching for medical device names with typos or variations. Add a smooth horizontal slide transition effect to the table content when switching between the six regulatory tabs in the main workspace. Integrate camera access to allow the user to scan device barcodes.  
Enable clickable rows in the data tables that expand to reveal hidden metadata fields (like factory address or full recall reasoning) currently omitted from the list view. Implement a button to generate a summary PDF report of the current audit results using the existing aiOutput data, formatted with proper headers and a timestamp. Add a text input field above each table to filter rows in real-time by keyword (e.g., searching for a specific device name or permit number). Add an 'Export as CSV' button to each registry table (in the header row) that utilizes the current filtered view of the dataset to generate and download a formatted CSV file. Add advanced search and filtering capabilities to the application. Users should be able to search across different fields, use boolean operators, and apply multiple filters simultaneously to refine their search results effectively.


Design a customizable dashboard for the application. Users should be able to add, remove, and rearrange various widgets (e.g., recent activity, key metrics, quick links). Ensure the dashboard layout is responsive and adapts to different screen sizes.

Add functionality for users to export their data in multiple formats (CSV, JSON, PDF) and import data from common file types. Ensure the import process is robust and handles potential data inconsistencies gracefully.

Please also adding 3 additional wow ai features to this app. Ending with 20 comprehensive follow up questions 
